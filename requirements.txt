# TetoMidi Hub - Python Dependencies
# NOTE: pyinstaller is a build tool, not a runtime dependency.
# Install it separately: pip install pyinstaller

# --- Audio / MIDI ---
mido>=1.3.0
python-rtmidi>=1.5.8
sounddevice>=0.4.6
soundfile>=0.12.1
librosa>=0.10.1

# --- ML / Transcription ---
piano_transcription_inference>=0.0.6  # ByteDance model
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
setuptools>=70.0.0  # FIX: Python 3.12+ cần setuptools mới để có pkg_resources ổn định
importlib_resources>=6.0  # FIX: Backup nếu thư viện nào đó đã migrate sang importlib

# --- GUI ---
PyQt6>=6.6.0

# --- Input / Automation ---
pynput>=1.7.6

# --- Utilities ---
requests>=2.31.0
packaging>=23.0
