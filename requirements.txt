# TetoMidi Hub - Python Dependencies
# NOTE: pyinstaller is a build tool, not a runtime dependency.
# Install it separately: pip install pyinstaller

# --- Audio / MIDI ---
mido>=1.3.0
python-rtmidi>=1.5.8
sounddevice>=0.4.6
soundfile>=0.12.1
librosa>=0.10.1

# --- ML / Transcription ---
transkun>=1.0.0
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
setuptools>=65.0  # cung cap pkg_resources ma transkun can luc runtime

# --- GUI ---
PyQt6>=6.6.0

# --- Input / Automation ---
pynput>=1.7.6

# --- Utilities ---
requests>=2.31.0
packaging>=23.0
